{
	"name": "flowlint-share-api",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@vitest/coverage-istanbul": "^4.0.0",
		"typescript": "^5.5.2",
		"vitest": "~4.0.0",
		"wrangler": "^4.56.0"
	}
}
